<UserControl x:Class="Knaeckebot.Controls.Actions.ClipboardActionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Knaeckebot.Controls.Actions"
             xmlns:converters="clr-namespace:Knaeckebot.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <GroupBox Header="Clipboard Details" Margin="0,0,0,5" Padding="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <!-- Options for text input -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Source:" Margin="0,0,5,5" 
                        VerticalAlignment="Center"/>
            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,5">
                <RadioButton Content="Direct text" 
                            Name="rbDirectText" 
                            Margin="0,0,10,0"
                            Checked="RbSourceType_Checked"/>
                <RadioButton Content="Use variable" 
                            Name="rbUseVariable"
                            Checked="RbSourceType_Checked"/>
            </StackPanel>
            <!-- Text input -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Text:" Margin="0,0,5,5" 
                        VerticalAlignment="Top" Name="lblText" Visibility="Collapsed"/>
            <TextBox Grid.Row="1" Grid.Column="1" Name="txtText" 
                        Margin="0,0,0,5" TextWrapping="Wrap" AcceptsReturn="True" Height="100"
                        Visibility="Collapsed"/>
            <!-- Variable selection -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Variable:" Margin="0,0,5,5" 
                        VerticalAlignment="Center" Name="lblVariable" Visibility="Collapsed"/>
            <ComboBox Grid.Row="2" Grid.Column="1" 
                    Name="cmbVariable"
                    Margin="0,0,0,5"
                    IsEditable="True"
                    Visibility="Collapsed"/>
            <TextBlock Grid.Row="3" Grid.Column="0" 
                        VerticalAlignment="Center"/>
            <CheckBox Grid.Row="3" Grid.Column="1" Content="Append to existing text" 
                    Name="chkAppend"
                    Margin="0,0,0,5"/>
            <TextBlock Grid.Row="4" Grid.Column="0" Text="Retries:" Margin="0,0,5,5" 
                        VerticalAlignment="Center"/>
            <TextBox Grid.Row="4" Grid.Column="1" Name="txtRetryCount" 
                    Width="80" HorizontalAlignment="Left"
                    Margin="0,0,0,5"/>
            <TextBlock Grid.Row="5" Grid.Column="0" Text="Wait time (ms):" Margin="0,0,5,5" 
                        VerticalAlignment="Center"/>
            <TextBox Grid.Row="5" Grid.Column="1" Name="txtRetryWaitTime" 
                    Width="80" HorizontalAlignment="Left"
                    Margin="0,0,0,5"/>
        </Grid>
    </GroupBox>
</UserControl>
